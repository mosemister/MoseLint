plugins {
    id 'application'
}

group 'org.moselint'
version '1.0.0'

application {
    mainClass = "org.moselint.MoseLint"
}

repositories {
    mavenCentral()
}

dependencies {
    implementation project(":OpenBlock-ProjectCreator")

    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.7.0'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.7.0'
}

test {
    useJUnitPlatform()
}